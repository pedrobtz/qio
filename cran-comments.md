## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

One local R-devel check reports an additional environment-only note because
the installed HTML Tidy is too old for HTML validation. The PDF manual and all
other documentation checks pass.

## Test environments

* Local macOS arm64, R-devel

## Downstream dependencies

There are currently no downstream dependencies.
