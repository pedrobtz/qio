% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{parquet_open}
\alias{parquet_open}
\title{Open a Parquet file}
\usage{
parquet_open(file, mmap = FALSE, verify_checksums = TRUE, threads = 0L)
}
\arguments{
\item{file}{Path to a Parquet file.}

\item{mmap}{Use memory-mapped input. On Windows a path the active code page
cannot represent is read with buffered input instead, because only the
mapped path needs a name that page can express. The result is the same.}

\item{verify_checksums}{Verify Parquet page checksums when present.}

\item{threads}{Number of reader threads. Zero picks the machine's core
count. \code{\link[=collect]{collect()}} decodes columns in parallel either way: a mapped file
shares one reader, and a buffered one gives each worker its own. Pass
\code{threads = 1} to force serial reads.}
}
\value{
A \code{qio_parquet_file} object. Close it with \code{\link[=parquet_close]{parquet_close()}}.
}
\description{
Opens a Parquet file for inexpensive metadata inspection and selective,
batched reading. The returned handle is valid only in the current R session.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(mtcars, path)
pf <- parquet_open(path)
dim(pf)
parquet_close(pf)
}
