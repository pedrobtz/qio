% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{read_parquet}
\alias{read_parquet}
\title{Read a Parquet file}
\usage{
read_parquet(
  file,
  ...,
  columns = NULL,
  row_groups = NULL,
  int64 = c("double", "integer64"),
  time = c("numeric", "hms"),
  tz = "UTC"
)
}
\arguments{
\item{file}{Path to a Parquet file.}

\item{...}{Must be empty. Every argument after it is name-only, matching
\code{\link[=collect]{collect()}}, \code{\link[=walk_batches]{walk_batches()}} and \code{\link[=read_plan]{read_plan()}}, which take the same
arguments the same way.}

\item{columns}{Character vector of complete column paths, or \code{NULL} (the
default) for all columns; see \code{\link[=collect]{collect()}}.}

\item{row_groups}{Integer vector of 1-based row-group IDs, or \code{NULL} (the
default) for all row groups.}

\item{int64}{How 64-bit integer columns reach R; see \code{\link[=collect]{collect()}}.}

\item{time}{How \code{TIME} columns reach R; see \code{\link[=collect]{collect()}}.}

\item{tz}{Time zone for \code{TIMESTAMP} columns; see \code{\link[=collect]{collect()}}.}
}
\value{
A data frame.
}
\description{
Reads an Apache Parquet file into a data frame.
}
\details{
Column types are mapped from Parquet as follows: \code{BOOLEAN} to logical,
\code{INT32} to integer, \code{INT64}/\code{FLOAT}/\code{DOUBLE} to double, and \code{BYTE_ARRAY}
(assumed UTF-8) to character. An \code{INT32} column annotated \code{DATE} is returned
as a \code{Date}, a UTC-adjusted \code{TIMESTAMP} (physical \code{INT64}) as a \code{POSIXct} in
UTC, and a legacy \code{INT96} timestamp as a \code{POSIXct} in UTC (interpreting its
Julian-day and nanosecond-of-day parts as an instant). Parquet nulls become
\code{NA}. \code{INT64} values are returned as doubles and lose precision beyond 2^53,
which for microsecond and nanosecond timestamps can drop sub-second precision
far from the epoch. Use \code{\link[=read_plan]{read_plan()}} to preview the R type of each column
before reading.

Nested and repeated columns are skipped with one message. Nested reading is
deferred to qio 0.2.0.

The file is memory-mapped for the duration of the read (falling back to
buffered reads if mapping fails) so columns decode in parallel; the mapping
is released before the function returns.

\code{columns} and \code{row_groups} read part of a file and are passed straight to
\code{\link[=collect]{collect()}}. Selecting columns is the single largest speedup available on a
wide file, because a column that is not selected is never decompressed.
Use \code{\link[=open_parquet]{open_parquet()}} with \code{\link[=collect]{collect()}} for the rest: \code{batch_size}, \code{mmap},
\code{threads}, and \code{verify_checksums}.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(data.frame(x = 1:3, y = c("a", "b", NA)), path)
read_parquet(path)
}
\seealso{
\code{\link[=write_parquet]{write_parquet()}}, \code{\link[=open_parquet]{open_parquet()}} and \code{\link[=collect]{collect()}} to read part of
a file, \code{\link[=walk_batches]{walk_batches()}} for a file larger than memory, and \code{\link[=read_plan]{read_plan()}}
to preview the R type of every column before reading.
}
