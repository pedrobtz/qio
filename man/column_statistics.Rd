% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{column_statistics}
\alias{column_statistics}
\alias{column_statistics.qio_parquet_file}
\title{Inspect Parquet column statistics}
\usage{
column_statistics(x, ...)

\method{column_statistics}{qio_parquet_file}(x, ...)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object.}

\item{...}{Reserved for future use.}
}
\value{
A data frame with one row per column chunk. \code{null_count} and
\code{distinct_count} are \code{NA} when the file does not record them.
}
\description{
Reports the per-column, per-row-group statistics recorded in the file: value
and null counts, and the minimum and maximum bounds.
}
\details{
These are \strong{claims made by whoever wrote the file}, not facts qio verifies.
A reader that skips a row group on them is trusting that writer. qio does not
use them to skip anything.

\code{min} and \code{max} are list columns, because one file can hold columns of
different types. Each element holds the bound decoded at the \emph{physical}
level: an \code{INT64} bound stays a number rather than becoming a \code{POSIXct}, and
a decimal is not scaled, since a bound is a sort key rather than a value to
compute with. Text columns are the exception and decode to character. An
element is \code{NULL} when the bound is absent, or is present but the wrong width
for its type.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(data.frame(n = 1:100), path, row_group_size = 25)
pf <- open_parquet(path)
stats <- column_statistics(pf)
stats[c("row_group", "name", "null_count")]
unlist(stats$min)
close_parquet(pf)
}
\seealso{
\code{\link[=column_chunks]{column_chunks()}}, \code{\link[=row_groups]{row_groups()}}
}
