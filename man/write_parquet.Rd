% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{write_parquet}
\alias{write_parquet}
\title{Write a Parquet file}
\usage{
write_parquet(
  x,
  file,
  compression = c("snappy", "zstd", "gzip", "lz4", "uncompressed"),
  schema = NULL,
  row_group_size = NULL,
  metadata = NULL,
  sorted_by = NULL,
  append = FALSE
)
}
\arguments{
\item{x}{A data frame (or a list of equal-length atomic vectors).}

\item{file}{Output path.}

\item{compression}{Compression codec: one of \code{"snappy"} (default), \code{"zstd"},
\code{"gzip"}, \code{"lz4"}, or \code{"uncompressed"}.}

\item{schema}{An optional schema created by \code{\link[=parquet_schema]{parquet_schema()}}. Named entries
override qio's inferred mapping; omitted columns retain automatic mapping.}

\item{row_group_size}{Rows per row group, or \code{NULL} (default) to write a
single row group. Smaller groups let other readers skip more but add
per-group metadata and can compress worse.}

\item{metadata}{A named character vector of footer key/value metadata, or
\code{NULL}. Duplicate keys are written in the order given. An \code{NA} value is
written as a key with no value, and reads back as \code{NA}.}

\item{sorted_by}{Columns the data is already sorted by, or \code{NULL}. Either a
character vector of column names, meaning ascending with nulls last, or a
data frame with a \code{name} column and optional logical \code{descending} and
\code{nulls_first} columns. This \strong{records a claim and nothing more}: qio does
not sort the data and does not check that the claim is true. A wrong
declaration misleads every reader that trusts it.}

\item{append}{Append new row groups to an existing file instead of replacing
it. The file must already exist and describe exactly the columns being
written; see Details.}
}
\value{
The output path, invisibly.
}
\description{
Writes a data frame to an Apache Parquet file.
}
\details{
Supported column types are logical, integer, double, character, and factor
(written as character). A column is written as nullable when it contains any
\code{NA}. \code{Date} columns are written as \code{INT32} with a \code{DATE} annotation, and
\code{POSIXct} columns as \code{INT64} microseconds with a UTC-adjusted \code{TIMESTAMP}
annotation; both round-trip back to their R class. Sub-microsecond fractions
of a second are rounded. Other classed columns are still written using their
underlying storage type and lose their class. An explicit \code{\link[=parquet_schema]{parquet_schema()}}
may instead select \code{INT64}, \code{FLOAT}, or a different timestamp unit, among the
supported declarations.

\code{append} adds row groups to a file that already exists, rather than replacing
it. Because it writes into data the user already has, qio checks
compatibility itself and refuses anything it cannot prove safe. The bundled
library compares column count, order, names, physical types, repetition type, and
logical type \emph{identity} -- but not logical \emph{parameters}, so it would happily
append microsecond timestamps to a millisecond file, or a decimal of one
scale to another. qio compares the full declaration, including those
parameters, and the schema path of every column.

Nullability is taken from the existing file rather than inferred from the new
data, so appending a batch that happens to contain no \code{NA} to a nullable
column works. Appending data that does contain \code{NA} to a column the file
declares \code{REQUIRED} is refused.

Row groups are the unit other readers skip on: a reader that can rule a group
out from its statistics never touches its pages. \code{row_group_size} sets how
many rows go in each. The default writes one row group, which keeps files
compact but leaves nothing to skip, so a file meant to be filtered by other
tools should set it.

\code{metadata} writes application key/value pairs into the footer, where
\code{\link[=metadata]{metadata()}} reads them back. Keys and values are stored as UTF-8 text;
Parquet defines no meaning for them.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(mtcars, path)

# Several row groups, with provenance in the footer.
write_parquet(
  mtcars,
  path,
  row_group_size = 8,
  metadata = c(source = "mtcars", written_by = "qio")
)
}
\seealso{
\code{\link[=read_parquet]{read_parquet()}}, \code{\link[=metadata]{metadata()}}, \code{\link[=row_groups]{row_groups()}}
}
