% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{bloom_filter_may_contain}
\alias{bloom_filter_may_contain}
\alias{bloom_filter_may_contain.qio_parquet_file}
\title{Test values against a Parquet bloom filter}
\usage{
bloom_filter_may_contain(x, column, values, row_group = 1L, ...)

\method{bloom_filter_may_contain}{qio_parquet_file}(x, column, values, row_group = 1L, ...)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object.}

\item{column}{A single complete column path, as \code{\link[=schema]{schema()}} reports it in
\code{path} and \code{\link[=names]{names()}} returns it -- not the bare \code{name}, which is not
unique across a nested file.}

\item{values}{Values to test. Numeric for numeric columns, character for
byte-array columns. \code{NA} returns \code{NA}.}

\item{row_group}{Row group to test, 1-based. A bloom filter belongs to one
column chunk, so it covers one row group.}

\item{...}{Reserved for future use.}
}
\value{
A logical vector the length of \code{values}: \code{FALSE} where the value is
definitely absent, \code{TRUE} where it may be present.
}
\description{
A bloom filter answers one question: is this value \emph{definitely absent} from
the column chunk? It never proves presence. \code{FALSE} means the value is not
there; \code{TRUE} means it may be, and only reading can settle it.
}
\details{
Values are matched against the column's physical type, because that is what
the writer hashed. A value qio cannot reduce to that type is an error rather
than a \code{FALSE}, which would read as "definitely absent".

qio's own writer does not emit bloom filters; see \link{qio-limitations}. Use
\code{\link[=column_chunks]{column_chunks()}} to find out whether a chunk has one.
}
\examples{
# qio's writer emits no bloom filters, so this uses a bundled file written
# by pyarrow. Its `key` column runs 0..3999 across four row groups.
path <- system.file("extdata", "bloom_sorted.parquet", package = "qio")
pf <- open_parquet(path)

# Row group 1 holds keys 0..999. A present value may be present; absent
# values are ruled out, and never wrongly, because there are no false
# negatives.
bloom_filter_may_contain(pf, "key", c(42, 123456))

# Which chunks even have a filter to test.
chunks <- column_chunks(pf)
chunks[chunks$row_group == 1, c("path", "bloom_filter")]

close_parquet(pf)
}
\seealso{
\code{\link[=column_chunks]{column_chunks()}}
}
