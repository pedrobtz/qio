% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{collect}
\alias{collect}
\alias{collect.qio_parquet_file}
\title{Collect data from a Parquet file}
\usage{
collect(x, ...)

\method{collect}{qio_parquet_file}(x, ..., columns = NULL, row_groups = NULL, batch_size = 65536L)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object.}

\item{...}{Reserved for future use.}

\item{columns}{Character vector of exact column paths, or \code{NULL} for all
columns.}

\item{row_groups}{Integer vector of 1-based row-group IDs, or \code{NULL} for all
row groups.}

\item{batch_size}{Positive number of rows decoded per batch.}
}
\value{
A data frame.
}
\description{
Columns are returned in requested order. Row groups are always returned in
their physical file order, even if their selector is not sorted.
\code{batch_size} controls decoding work; it does not bound the memory occupied by
the final data frame. Use \code{\link[=walk_batches]{walk_batches()}} for bounded-memory processing.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(mtcars, path)
pf <- parquet_open(path)
collect(pf, columns = c("mpg", "cyl"))
parquet_close(pf)
}
