% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{collect}
\alias{collect}
\alias{collect.qio_parquet_file}
\title{Collect data from a Parquet file}
\usage{
collect(x, ...)

\method{collect}{qio_parquet_file}(
  x,
  ...,
  columns = NULL,
  row_groups = NULL,
  batch_size = 65536L,
  int64 = c("double", "integer64"),
  time = c("numeric", "hms"),
  tz = "UTC",
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object.}

\item{...}{Reserved for future use.}

\item{columns}{Character vector of complete column paths, or \code{NULL} for all
columns. Paths are matched exactly as \code{\link[=schema]{schema()}} and \code{\link[=names]{names()}} report
them, dot-separated for nested leaves, and never by leaf name alone: two
leaves may share a name under different parents. An unknown path is an
error, and so is a path matching more than one leaf.}

\item{row_groups}{Integer vector of 1-based row-group IDs, or \code{NULL} for all
row groups.}

\item{batch_size}{Positive number of rows decoded at a time. It bounds the
reader's scratch memory, not the size of the result (see Details);
\code{\link[=walk_batches]{walk_batches()}} additionally uses it as the size of each batch.}

\item{int64}{How 64-bit integer columns reach R. \code{"double"} (the default)
is exact from \code{-2^53} through \code{2^53} and returns \code{NA} outside it.
\code{"integer64"} returns \link[bit64:integer64]{bit64::integer64}, which covers the full signed
64-bit range, and needs the suggested \code{bit64} package. Either way values
that cannot be represented become \code{NA} and one warning is emitted per
read. Unsigned 64-bit columns are never returned as negative numbers.}

\item{time}{How \code{TIME} columns reach R: \code{"numeric"} (the default) returns
seconds since midnight, \code{"hms"} returns \link[hms:hms]{hms::hms} and needs the suggested
\code{hms} package. Neither returns \code{POSIXct}, because a time of day is not an
instant.}

\item{tz}{Time zone name, \code{"UTC"} by default. A UTC-adjusted \code{TIMESTAMP} is
an instant, so \code{tz} changes only how it prints. A non-UTC \code{TIMESTAMP} is a
wall clock with no zone stored, so its civil components are interpreted in
\code{tz}; base R decides ambiguous and nonexistent times at daylight-saving
boundaries. The machine's local zone is never used implicitly.}

\item{verbose}{Report what the read is about to do before doing it: the
rows, columns, and row groups selected, the batch size, and the resolved
\code{\link[=read_plan]{read_plan()}} for the selected columns only -- not for the whole file, so
it answers "what am I about to get". Written with \code{message()}, so it goes
to stderr and \code{suppressMessages()} silences it.}
}
\value{
A data frame.
}
\description{
Columns are returned in requested order. Row groups are always returned in
their physical file order, even if their selector is not sorted.
}
\details{
\code{collect()} returns the whole selection, so \code{batch_size} does not bound the
result; use \code{\link[=walk_batches]{walk_batches()}} for that. It does bound the scratch memory the
reader allocates while decoding string and binary columns, which would
otherwise scale with the largest selected row group rather than with
anything the caller controls. Smaller batches lower peak memory and cost a
little throughput on dictionary-encoded text.

Nested and repeated columns are not materialized in qio 0.1.0. When a
selection includes them, they are omitted and one message reports how many
physical leaf columns were skipped. Nested reading is deferred to qio 0.2.0.
If every selected column is nested, the result is a zero-column data frame
with the selected number of rows.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(mtcars, path)
pf <- open_parquet(path)
collect(pf, columns = c("mpg", "cyl"))
close_parquet(pf)
}
\seealso{
\code{\link[=open_parquet]{open_parquet()}} for the handle and for \code{mmap} and \code{threads},
\code{\link[=walk_batches]{walk_batches()}} to process a file that does not fit in memory,
\code{\link[=read_plan]{read_plan()}} to preview the R type of every column, and \code{\link[=read_parquet]{read_parquet()}},
which is \code{\link[=open_parquet]{open_parquet()}} plus \code{collect()} for a whole file.
}
