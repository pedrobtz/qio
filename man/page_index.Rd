% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{page_index}
\alias{page_index}
\alias{page_index.qio_parquet_file}
\title{Inspect Parquet page indexes}
\usage{
page_index(x, ...)

\method{page_index}{qio_parquet_file}(x, ...)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object.}

\item{...}{Reserved for future use.}
}
\value{
A data frame with one row per page. \code{first_row}, \code{offset}, and
\code{compressed_bytes} come from the offset index and are \code{NA} when only a
column index is present; \code{null_count}, \code{null_page}, \code{min}, and \code{max} come
from the column index and are \code{NA} or \code{NULL} when only an offset index is.
\code{min} and \code{max} are list columns, decoded as in \code{\link[=column_statistics]{column_statistics()}}.
}
\description{
Reports the per-page index of each column chunk: where each data page sits
in the file, which row it starts at, and the bounds and null count it
declares. One row per page.
}
\details{
A page index is optional, and many writers omit it. Columns without one
contribute no rows, so a file with no page index at all returns a
zero-row data frame rather than an error. qio's own writer does not emit
page indexes; see \link{qio-limitations}.

Like \code{\link[=column_statistics]{column_statistics()}}, the bounds are claims made by whoever wrote the
file. qio reports them and does not use them to skip pages.
}
\examples{
# qio does not write page indexes, so a file it wrote has none and the
# result is empty rather than an error.
path <- tempfile(fileext = ".parquet")
write_parquet(data.frame(n = 1:10), path)
pf <- parquet_open(path)
nrow(page_index(pf))
parquet_close(pf)
}
\seealso{
\code{\link[=column_statistics]{column_statistics()}}, \code{\link[=column_chunks]{column_chunks()}}
}
