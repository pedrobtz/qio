% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-plan.R
\name{read_plan}
\alias{read_plan}
\alias{read_plan.qio_parquet_file}
\alias{read_plan.character}
\alias{read_plan.data.frame}
\title{Plan how a Parquet file is read into R}
\usage{
read_plan(x, ...)

\method{read_plan}{qio_parquet_file}(
  x,
  ...,
  int64 = c("double", "integer64"),
  time = c("numeric", "hms"),
  tz = "UTC"
)

\method{read_plan}{character}(
  x,
  ...,
  int64 = c("double", "integer64"),
  time = c("numeric", "hms"),
  tz = "UTC"
)

\method{read_plan}{data.frame}(
  x,
  ...,
  int64 = c("double", "integer64"),
  time = c("numeric", "hms"),
  tz = "UTC"
)
}
\arguments{
\item{x}{A Parquet file path, a \code{qio_parquet_file} object, or the data frame
returned by \code{\link[=schema]{schema()}}.}

\item{...}{Reserved for future use.}

\item{int64}{How 64-bit integer columns reach R; see \code{\link[=collect]{collect()}}. The plan
reports the resulting \code{r_type} and \code{converter}, so it can be inspected for
exactly the read that will follow.}

\item{time}{How \code{TIME} columns reach R; see \code{\link[=collect]{collect()}}.}

\item{tz}{Time zone for \code{TIMESTAMP} columns; see \code{\link[=collect]{collect()}}.}
}
\value{
A \code{qio_read_plan} data frame with one row per physical leaf column
and the columns:
\describe{
\item{\code{column}}{1-based physical column index.}
\item{\code{name}, \code{path}}{Column name and dotted path.}
\item{\code{physical_type}, \code{logical_type}}{Parquet physical type and logical
annotation (\code{NA} when absent).}
\item{\code{r_type}}{Target R type, or \code{NA} when the column cannot be
collected.}
\item{\code{converter}}{Stable identifier of the conversion the reader uses.}
\item{\code{nullable}}{Whether the column can contain nulls.}
\item{\code{nested}}{Whether the leaf belongs to a nested or repeated field.}
\item{\code{collectible}}{Whether \code{\link[=collect]{collect()}} can currently materialize the
column.}
\item{\code{note}}{Reason a column is not collectible, or a pending logical
annotation; \code{NA} otherwise.}
}
}
\description{
Builds a read plan from a Parquet schema: one row per physical leaf column
describing the R type each column will materialize as, whether it can be
collected, and why not when it cannot. The plan is a pure function of the
schema, so it is cheap to compute and inspect before reading any data.
}
\details{
The plan reflects what \code{\link[=collect]{collect()}}, \code{\link[=read_parquet]{read_parquet()}}, and \code{\link[=walk_batches]{walk_batches()}}
actually do today. Supported logical annotations override the physical type:
\code{DATE}, UTC-adjusted \code{TIMESTAMP}, and legacy physical \code{INT96} timestamps are
converted to their R date-time classes. Unimplemented annotations are
reported in \code{note} and retain their physical fallback type.

A path is enough -- \code{read_plan()} opens the file, reads the footer, and
closes it again, so no handle is needed to inspect a file before reading it.
Passing an open \code{\link[=open_parquet]{open_parquet()}} handle, or the data frame from \code{\link[=schema]{schema()}},
produces exactly the same plan; use those when a handle is already open or
when the schema has already been fetched.

Pass the same \code{int64}, \code{time}, and \code{tz} the read will use. The plan resolves
them, so \code{r_type} and \code{converter} describe that read rather than a default
one.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(data.frame(x = 1:3, y = c("a", "b", NA)), path)

# A path is enough; no handle is needed.
read_plan(path)

# The plan answers for the read you are about to do, not a default one.
read_plan(path, int64 = "integer64")

# An open handle and a schema() data frame give the same plan.
pf <- open_parquet(path)
identical(read_plan(pf), read_plan(path))
close_parquet(pf)
}
\seealso{
\code{\link[=schema]{schema()}}, \code{\link[=collect]{collect()}}, \code{\link[=parquet_type_mapping]{parquet_type_mapping()}}
}
