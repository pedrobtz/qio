% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-plan.R
\name{read_plan}
\alias{read_plan}
\alias{read_plan.qio_parquet_file}
\alias{read_plan.data.frame}
\title{Plan how a Parquet file is read into R}
\usage{
read_plan(x, ...)

\method{read_plan}{qio_parquet_file}(x, ...)

\method{read_plan}{data.frame}(x, ...)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object, or the data frame returned by
\code{\link[=schema]{schema()}}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{qio_read_plan} data frame with one row per physical leaf column
and the columns:
\describe{
\item{\code{column}}{1-based physical column index.}
\item{\code{name}, \code{path}}{Column name and dotted path.}
\item{\code{physical_type}, \code{logical_type}}{Parquet physical type and logical
annotation (\code{NA} when absent).}
\item{\code{r_type}}{Target R type, or \code{NA} when the column cannot be
collected.}
\item{\code{converter}}{Stable identifier of the conversion the reader uses.}
\item{\code{nullable}}{Whether the column can contain nulls.}
\item{\code{collectible}}{Whether \code{\link[=collect]{collect()}} can currently materialize the
column.}
\item{\code{note}}{Reason a column is not collectible, or a pending logical
annotation; \code{NA} otherwise.}
}
}
\description{
Builds a read plan from a Parquet schema: one row per physical leaf column
describing the R type each column will materialize as, whether it can be
collected, and why not when it cannot. The plan is a pure function of the
schema, so it is cheap to compute and inspect before reading any data.
}
\details{
The plan reflects what \code{\link[=collect]{collect()}}, \code{\link[=read_parquet]{read_parquet()}}, and \code{\link[=walk_batches]{walk_batches()}}
actually do today. Read types are currently chosen from the Parquet
\emph{physical} type; logical annotations such as \code{DATE}, \code{TIMESTAMP}, and
\code{DECIMAL} are reported but not yet applied to the materialized values. When a
collectible column carries such an annotation, the \code{note} column records that
it is not yet applied.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(data.frame(x = 1:3, y = c("a", "b", NA)), path)
pf <- parquet_open(path)
read_plan(pf)
parquet_close(pf)
}
\seealso{
\code{\link[=schema]{schema()}}, \code{\link[=collect]{collect()}}, \code{\link[=parquet_type_mapping]{parquet_type_mapping()}}
}
