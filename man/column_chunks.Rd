% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-file.R
\name{column_chunks}
\alias{column_chunks}
\alias{column_chunks.qio_parquet_file}
\title{Inspect Parquet column chunks}
\usage{
column_chunks(x, ...)

\method{column_chunks}{qio_parquet_file}(x, ...)
}
\arguments{
\item{x}{A \code{qio_parquet_file} object.}

\item{...}{Reserved for future use.}
}
\value{
A data frame with one row per column chunk. \code{encodings} lists the
encodings the chunk declares, comma separated. \code{page_index} is \code{TRUE} when
either a column index or an offset index is present.
}
\description{
Reports how each column is stored in each row group: its physical type,
compression, sizes, encodings, and which optional structures are present.
One row per column per row group.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet(mtcars, path, row_group_size = 16)
pf <- open_parquet(path)
column_chunks(pf)
close_parquet(pf)
}
\seealso{
\code{\link[=column_statistics]{column_statistics()}}, \code{\link[=row_groups]{row_groups()}}
}
