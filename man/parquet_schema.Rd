% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-schema.R
\name{parquet_schema}
\alias{parquet_schema}
\title{Create a Parquet writer schema}
\usage{
parquet_schema(...)
}
\arguments{
\item{...}{Named Parquet type specifications.}
}
\value{
A \code{qio_parquet_schema} data frame.
}
\description{
Creates a reusable schema that controls how \code{\link[=write_parquet]{write_parquet()}} stores selected
columns. Each argument must be named and may be a type string or a list whose
first element is the type. Schemas may be partial: unspecified columns keep
qio's automatic mapping.
}
\details{
Supported declarations are \code{"AUTO"}, \code{"BOOLEAN"}, \code{"INT32"}, \code{"INT64"},
\code{"FLOAT"}, \code{"DOUBLE"}, \code{"STRING"}, \code{"DATE"}, and \code{"TIMESTAMP"}.
\code{TIMESTAMP} accepts \code{unit} (\code{"MILLIS"}, \code{"MICROS"}, or \code{"NANOS"}) and must
be adjusted to UTC. All types accept \code{repetition_type} (\code{"AUTO"},
\code{"REQUIRED"}, or \code{"OPTIONAL"}).

\code{INT32} and \code{INT64} declarations accept finite whole-number inputs; \code{INT64}
is limited to R's exact double-integer range from \code{-2^53} through \code{2^53}.
\code{FLOAT} and \code{DOUBLE} accept numeric input, \code{STRING} accepts character or
factor input, \code{DATE} accepts \code{Date} or whole-number days, and \code{TIMESTAMP}
accepts \code{POSIXct}.
}
\examples{
parquet_schema(
  id = "INT64",
  price = "FLOAT",
  created_at = list("TIMESTAMP", unit = "MILLIS")
)
}
